class Solution(object):
    def lengthOfLastWord(self, s):
        """
        :type s: str
        :rtype: int
        """
        if len(s) == 0 or s == None:
            return 0
            
        a = s.strip().split(" ")
        return len(a[-1])
        
